export enum payment_type {
  cash = 0,
  pos = 1,
  bank = 2,
  momo = 3,
}

export enum readonly_type {
  nomal = 0,
  readonly = 1,
}

export enum form_type {
  create = 0,
  edit = 1,
  delete = 2,
  readonly = 3,
}